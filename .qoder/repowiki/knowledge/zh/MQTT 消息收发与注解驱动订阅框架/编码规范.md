- 采用类级别的 @MqttController 定义父 Topic，方法级别的 @MqttTopicMapping 定义子 Topic，组合形成完整的订阅路径。
- 所有 MQTT 业务处理器必须标注 @Component 以确保被 Spring 容器管理，从而能被 MqttBeanPostProcessor 捕获并注册。
- 消息发送默认采用 JSON 格式序列化对象，并提供统一的 Qos 枚举（QOS_0, QOS_1, QOS_2）进行质量控制。