- 所有动态创建的 DataSource 实例均包装为 DruidDataSource，并在销毁或替换时显式调用 close() 释放资源。
- 数据源创建过程采用 Key 级别的 CompletableFuture 锁机制，避免高并发下对同一 Key 重复创建连接池。
- 通过 ThreadLocal 存储当前线程的数据源标识，并强制要求在 AOP 切面的 finally 块中执行 clear() 操作。