- 采用扁平化包结构，按功能域划分为 `tools`（加解密）、`utils`（通用辅助）和 `bean`（数据模型）。
- 核心设计遵循无状态模式，所有工具类均通过 `public static` 方法提供服务，不依赖实例化。
- 内部存在明确的依赖层级：基础工具类（如 `Base64Tool`）被上层加密工具（如 `AesTool`）调用，形成自包含的工具链。
- 集成 `hutool-all`、`fastjson2` 及 `zxing` 等第三方库以增强特定领域能力，同时保留自定义实现以满足特定业务逻辑（如 `SnowflakeIdWorker`）。