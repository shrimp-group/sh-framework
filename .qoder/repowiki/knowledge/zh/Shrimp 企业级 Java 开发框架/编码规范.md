- 所有子模块均遵循 Spring Boot 4.0 自动装配规范，通过 SPI 文件注册配置类而非传统 xml 或 ComponentScan。
- 全局采用 `com.wkclz` 作为根包名，并按功能域划分为 core, web, mybatis 等独立命名空间。
- 统一使用 `flatten-maven-plugin` 处理 CI 友好的版本号（${revision}），确保发布产物 POM 的简洁性。