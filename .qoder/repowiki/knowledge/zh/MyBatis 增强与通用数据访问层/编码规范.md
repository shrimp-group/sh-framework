- 所有 Mapper 接口继承 BaseMapper 并指定 BaseEntity 泛型，通过 Provider 类动态构建 SQL 而非 XML 映射。
- 查询拦截器统一将请求参数中的空字符串转换为 null，避免数据库查询产生非预期的空串匹配。
- 更新拦截器在 Executor 层面拦截 INSERT/UPDATE 操作，自动从 UserContext 获取当前用户并填充审计字段。
- 动态 SQL 构建时强制校验 ORDER BY 字段名，仅允许实体中定义的合法列名，防止 SQL 注入风险。