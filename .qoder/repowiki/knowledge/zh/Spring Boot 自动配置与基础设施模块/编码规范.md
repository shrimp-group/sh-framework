- 敏感配置采用 ENC(...) 统一格式标识，支持 RSA 混合加密（信封加密）与 AES 对称加密两种解密路径。
- 解密密钥优先通过环境变量（如 SH_CONFIG_KEYSTORE_PASSWORD）注入，避免硬编码在配置文件中以提升安全性。
- 工具类（如 SensitiveConfigDecryptor, SnowflakeHelper）多采用静态方法设计，部分核心组件（如 SpringContextHolder）通过静态变量持有容器引用以方便全局调用。