- 所有 Redis 操作工具类方法均采用 try-catch 包裹，异常时记录日志并返回 null 或 false，确保调用方不因 Redis 故障而崩溃。
- 涉及状态变更或多步校验的 Redis 操作（如分布式锁释放、续期）均通过 Lua 脚本保证原子性执行。
- 分布式锁与 ID 生成器等有状态组件采用双重检查锁定（DCL）模式实现懒加载初始化，确保线程安全。
- 自定义序列化器通过构造函数注入可配置的白名单列表，并与默认框架白名单合并，实现灵活且安全的 AutoType 过滤。