- 所有 REST 接口统一返回泛型包装类 `R<T>`，确保前后端交互格式一致。
- Controller 层通过 `BeanUtils.copyProperties` 在 Entity 与 Req/Resp VO 之间进行显式属性转换，避免直接暴露实体类。
- API 路径常量集中定义在 `Route` 接口中，并配合 Swagger `@Operation` 注解提供接口文档描述。
- Service 层通过继承框架提供的 `BaseService<Entity, Mapper>` 泛型基类，零代码实现标准 CRUD 功能。